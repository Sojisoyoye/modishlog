"""Auth dependencies for FastAPI Depends injection."""

# TODO: get_current_user(token: str = Depends(oauth2_scheme)) -> User
# TODO: get_current_active_user(user: User = Depends(get_current_user)) -> User
