"""Auth Pydantic schemas -- request/response validation."""

# TODO: LoginRequest(BaseModel) -- email, password
# TODO: TokenResponse(BaseModel) -- access_token, refresh_token, token_type
# TODO: RegisterRequest(BaseModel) -- email, password, full_name
# TODO: UserResponse(BaseModel) -- id, email, full_name, is_active
#        model_config = ConfigDict(from_attributes=True)
