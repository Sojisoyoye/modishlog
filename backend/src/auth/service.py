"""Auth business logic -- all async operations."""

# TODO: authenticate_user(email, password) -> User
# TODO: create_tokens(user_id) -> TokenPair
# TODO: refresh_access_token(refresh_token) -> str
# TODO: revoke_token(token) -> None
# TODO: register_user(data) -> User
# TODO: initiate_password_reset(email) -> None
