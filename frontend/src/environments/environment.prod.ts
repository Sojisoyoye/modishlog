export const environment = {
  production: true,
  apiBaseUrl: 'https://api.modishlog.com/api/v1',
  // See environment.ts for rationale -- empty until a real Turnstile site
  // key exists.
  turnstileSiteKey: '',
};
