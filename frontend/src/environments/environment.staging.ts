export const environment = {
  production: false,
  apiBaseUrl: 'https://api.staging.modishlog.com/api/v1',
  // See environment.ts for rationale -- empty until a real Turnstile site
  // key exists.
  turnstileSiteKey: '',
};
