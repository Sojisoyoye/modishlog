{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run build; else sed -i \"s|__STAGING_API_URL__|$STAGING_API_URL|g\" src/environments/environment.staging.ts && npm run build:staging; fi",
  "outputDirectory": "dist/frontend/browser",
  "installCommand": "npm ci",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
